# AWS Configuration for SES Email Service
VITE_AWS_REGION=us-east-1
VITE_AWS_ACCESS_KEY_ID=your_aws_access_key_id
VITE_AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key
VITE_SES_FROM_EMAIL=noreply@yourcompany.com
VITE_SES_FROM_NAME=Procurement System

# Instructions:
# 1. Copy this file to .env
# 2. Replace the placeholder values with your actual AWS credentials
# 3. Ensure your AWS SES is configured and verified
# 4. Make sure the from email is verified in AWS SES
# 5. If not configured, the app will fall back to mock email service